import React from "react";
import Form from "../componentes/form";
import Carrusel from "../componentes/carrusel";

const Home = () => (
  <section>
    <Carrusel />
    <Form />
  </section>
);

export default Home;
